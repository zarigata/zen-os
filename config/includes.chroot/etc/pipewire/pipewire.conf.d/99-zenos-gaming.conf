# ZEN-OS PipeWire Gaming Audio Configuration
# Lower latency for gaming while maintaining stability

context.properties = {
    default.clock.rate = 48000
    default.clock.quantum = 256
    default.clock.min-quantum = 128
    default.clock.max-quantum = 2048
}

# For ultra-low latency gaming (via game-mode-audio script):
# quantum = 128 → ~2.7ms at 48kHz
# Default: 256 → ~5.3ms at 48kHz (good balance)
