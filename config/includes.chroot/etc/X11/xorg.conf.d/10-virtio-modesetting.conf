# Force modesetting driver for QEMU virtio-gpu
# Required for Xorg to start on QEMU VMs with -vga virtio
Section "Device"
    Identifier "Virtio GPU"
    Driver     "modesetting"
    Option     "AccelMethod" "none"
EndSection
