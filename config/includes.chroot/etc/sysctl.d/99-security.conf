# /etc/sysctl.d/99-security.conf — ZEN-OS security kernel hardening
# Applied in addition to 99-gaming.conf

# === Network Security ===

# Disable IP forwarding (not a router)
net.ipv4.ip_forward=0
net.ipv6.conf.all.forwarding=0

# Enable SYN cookies (SYN flood protection)
net.ipv4.tcp_syncookies=1

# Disable IP source routing
net.ipv4.conf.all.accept_source_route=0
net.ipv4.conf.default.accept_source_route=0
net.ipv6.conf.all.accept_source_route=0

# Disable ICMP redirects
net.ipv4.conf.all.accept_redirects=0
net.ipv4.conf.default.accept_redirects=0
net.ipv6.conf.all.accept_redirects=0
net.ipv6.conf.default.accept_redirects=0

# Don't send ICMP redirects
net.ipv4.conf.all.send_redirects=0
net.ipv4.conf.default.send_redirects=0

# Reverse path filtering
net.ipv4.conf.all.rp_filter=1
net.ipv4.conf.default.rp_filter=1

# Log martian packets
net.ipv4.conf.all.log_martians=1
net.ipv4.conf.default.log_martians=1

# Ignore ICMP broadcast requests
net.ipv4.icmp_echo_ignore_broadcasts=1

# Ignore bogus ICMP error responses
net.ipv4.icmp_ignore_bogus_error_responses=1

# === Kernel Security ===

# Enable ASLR (address space layout randomization) — max
kernel.randomize_va_space=2

# Restrict dmesg to root
kernel.dmesg_restrict=1

# Restrict kernel pointer access
kernel.kptr_restrict=2

# Restrict unprivileged use of BPF
kernel.unprivileged_bpf_disabled=1

# Enable exec shield equivalent
kernel.exec-shield=1

# Core dump restrictions
fs.suid_dumpable=0

# Protected hardlinks and symlinks
fs.protected_hardlinks=1
fs.protected_symlinks=1
fs.protected_fifos=2
fs.protected_regular=2

# === Memory Protection ===

# Disable unprivileged memory mapping at low addresses
vm.mmap_min_addr=65536

# === File System ===

# Maximum number of file watchers (dev/gaming workload)
fs.inotify.max_user_watches=524288
